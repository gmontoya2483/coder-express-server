<div class="table table-responsive">
    <table class="table table-dark">
        <tr style="color: yellow;">
            <th>Título</th>
            <th>Precio</th>
            <th>Imagen</th>
        </tr>
        {{#each products}}
            <tr>
                <td>{{ this.title }}</td>
                <td>{{ this.price }}</td>
                <td>
                    <img width="30px" src="{{ this.thumbnail}}" alt="{{ this.title }}">
                </td>
            </tr>
        {{/each}}
    </table>
</div>
